const sum = (x, y) => {
    return x + y;
}

const difference = (x, y) => {
    return x - y;
}

const product = (x, y) => {
    return x * y;
}

const quotient = (x, y) => {
    return x / y;
}

const PEMDAS = (a, b, c) => {
    return (a+b) / 22 * (21-245);
}

let a = 1;
let b = 2

console.log(`PEMDAS result: ${PEMDAS(a, b, 12)}`);